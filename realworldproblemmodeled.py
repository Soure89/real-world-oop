class Employees(object):
	def __init__(self,Type_,salary,name="not_found"):
		self.salary = salary
		self.name = name
		self.Type_ = Type_
		self.salary = salary

		if self.name != ("eric",5662,2323):
			return False
		else:
			return True


			"""The employee should be called `not_found` if no name was passed as an argument"""
			def is_eric(self):
				if self.Type_ == 'agent':
					return True
				else:
					return "not_found"















	
